services:
  postgres:
    image: pgvector/pgvector:pg15
    platform: linux/amd64
    container_name: agentic_postgres_cybersecurity
    restart: always
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: agentic_cybersecurity_db
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U myuser -d agentic_cybersecurity_db"]
      interval: 5s
      retries: 5
    networks:
      - agentic_net

  pgadmin:
    image: dpage/pgadmin4
    platform: linux/amd64
    container_name: agentic_pgadmin_cybersecurity
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@agentic.com
      PGADMIN_DEFAULT_PASSWORD: 1234
    ports:
      - "5051:80"
    depends_on:
      - postgres
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    networks:
      - agentic_net

  memory-service:
    build: .
    container_name: agentic_memory_service
    working_dir: /app
    command: uvicorn memory.main:app --host 0.0.0.0 --port 8000
    ports:
      - "1000:8000"
    environment:
      MEMORY_DB_URL: postgresql://myuser:mypassword@postgres:5432/agentic_cybersecurity_db
      PYTHONPATH: /app
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - agentic_net

# example something service
  something-service:
    build:
      context: .
      dockerfile: Dockerfile
    image: agentic-security-local:latest
    container_name: something-service
    working_dir: /app
    command: uvicorn services.something_service.api:app --host 0.0.0.0 --port 9000
    ports:
      - "9000:9000"
    env_file:
      - .env

  something-server:
    image: agentic-stocks-local:latest
    container_name: mcp-something-server
    working_dir: /app
    command: python -m mcp_servers.something_server
    depends_on:
      - something-service
    environment:
      - SOMETHING_SERVICE_URL=http://something-service:9000
    ports:
      - "8000:8000"
    env_file:
      - .env

volumes:
  db_data:
  pgadmin_data:

networks:
  agentic_net: